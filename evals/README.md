# evals

Golden datasets + eval harnesses. A no-op placeholder in Phase 0; populated by:
- Phase 2 — matching precision/recall (`evals/matching/`)
- Phase 3 — detection opportunity precision/recall (`evals/detection/`)
- Phase 6 — NirvanaI answer faithfulness (`evals/faithfulness/`)

`run_evals.py` is the CI entrypoint and currently exits 0 with "no evals yet".
